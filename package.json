{
  "name": "omeka-s-themes",
  "version": "0.1.0",
  "private": true,
  "description": "Static GitHub Pages catalog of Omeka S themes hosted on GitHub.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node --env-file-if-exists=.env scripts/build.mjs"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
